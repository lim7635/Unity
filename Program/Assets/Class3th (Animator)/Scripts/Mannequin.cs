using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Mannequin : MonoBehaviour
{
    // 값 타입 : byte, char, bool, short, int
    // long, float, double, decimal, struct 등

    // 참조 타입 : class, array, collection, delegate(함수 포인터),
    // interface, abstractclass(추상 클래스), event 등

    // object : 값 & 참조 타입 모두 사용할 수 있는 자료형(참조 타입)
}